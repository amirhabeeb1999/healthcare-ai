version: 1.0
runtime: nodejs18
build:
  commands:
    - npm install
    - npm run build
run:
  runtime-version: 18
  command: npm start
  environment:
    - name: NODE_ENV
      value: production
    - name: PORT
      value: 3000
  network:
    port: 3000

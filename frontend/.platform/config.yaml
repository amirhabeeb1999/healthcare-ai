version: 1.0

# Build configuration
build:
  commands:
    - npm install
    - npm run build

# Run configuration
run:
  command: npm start
  network:
    port: 3000
  
# Environment variables
environment:
  - name: NODE_ENV
    value: production
  - name: PORT
    value: 3000

# Runtime
runtime: nodejs18
